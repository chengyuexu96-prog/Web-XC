<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>雅集 · 社区 - 瓷韵中华</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Ma+Shan+Zheng&family=Noto+Serif+SC:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/community.css">
</head>
<body>
    
    <%- include('partials/header') %>

    <div class="container pt-5 mt-5">
        <div class="pt-4 mb-4">
            <h1 class="brand-font" style="color: var(--gold-primary); font-size: 4rem; letter-spacing: 4px;">雅 集 · 赏 瓷</h1>
            <p class="text-secondary">品瓷赏玉，记录指尖下的流光溢彩</p>
        </div>
        
        <div class="masonry-container" id="feedContainer">
            <% posts.forEach(function(post) { %>
                <div class="note-card" onclick="openDetail('<%= post.id %>')">
                    <% 
                        let imgUrl = '';
                        try {
                            const parsed = JSON.parse(post.images);
                            imgUrl = Array.isArray(parsed) && parsed.length > 0 ? parsed[0] : '';
                        } catch(e) { imgUrl = post.images; } // 兼容旧数据
                    %>
                    <img src="<%= imgUrl %>" class="note-img" loading="lazy" onerror="this.src='https://placehold.co/600x400/333/999?text=无图'">
                    
                    <div class="note-info">
                        <div class="note-title"><%= post.title %></div>
                        <div class="user-row">
                            <div class="d-flex align-items-center gap-1">
                                <% if(post.user_avatar && post.user_avatar.includes('/uploads/')) { %>
                                    <img src="<%= post.user_avatar %>" style="width:24px;height:24px;border-radius:50%;object-fit:cover;">
                                <% } else { %>
                                    <div class="bg-warning text-dark rounded-circle text-center fw-bold" style="width:24px;height:24px;line-height:24px;font-size:0.7rem;">
                                        <%= post.username.charAt(0) %>
                                    </div>
                                <% } %>
                                <span class="small"><%= post.username %></span>
                            </div>
                            <span><i class="bi bi-heart"></i> <%= post.likes_count %></span>
                        </div>
                    </div>
                </div>
            <% }); %>
            
            <% if(posts.length === 0) { %>
                <div class="text-center text-muted py-5 w-100">
                    <p>暂无雅集，待您首发...</p>
                </div>
            <% } %>
        </div>
    </div>

    <div class="fab-btn" onclick="openPublishModal()"><i class="bi bi-plus"></i></div>

    <div class="modal fade" id="detailModal" tabindex="-1">
        <div class="modal-dialog modal-xl modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-body">
                    <div class="modal-left">
                        <div class="swiper detailSwiper h-100">
                            <div class="swiper-wrapper" id="detailImages"></div>
                            <div class="swiper-pagination"></div>
                            <div class="swiper-button-next" style="color:var(--gold-primary)"></div>
                            <div class="swiper-button-prev" style="color:var(--gold-primary)"></div>
                        </div>
                    </div>
                    <div class="modal-right">
                        <button type="button" class="btn-close btn-close-white position-absolute top-0 end-0 m-3 z-3" data-bs-dismiss="modal"></button>
                        
                        <div class="author-header d-flex justify-content-between align-items-center">
                            <div class="d-flex align-items-center gap-3">
                                <div class="position-relative">
                                    <img id="detailAvatarImg" src="" style="width:48px;height:48px;border-radius:50%;object-fit:cover; display:none; border:2px solid var(--gold-primary)">
                                    <div id="detailAvatarTxt" class="bg-warning text-dark rounded-circle text-center fw-bold" style="width:48px;height:48px;line-height:48px; font-size:1.2rem;"></div>
                                </div>
                                <div>
                                    <div class="fw-bold fs-5" id="detailUsername">User</div>
                                    <div class="text-secondary small">同好</div>
                                </div>
                            </div>
                            <button class="btn btn-outline-warning btn-sm rounded-pill px-3" id="btnFollow" onclick="toggleFollow()">关注</button>
                        </div>

                        <div class="post-content-scroll">
                            <h4 class="mb-3" style="color:var(--gold-primary); font-family: 'Ma Shan Zheng';" id="detailTitle"></h4>
                            <div id="detailContent" class="text-light-50 lh-lg" style="white-space: pre-wrap; font-size:0.95rem; opacity:0.9"></div>
                            <hr class="border-secondary opacity-25">
                            <h6 class="text-secondary mb-3 small">评注 · <span id="commentTotal">0</span></h6>
                            <div id="commentList"></div>
                        </div>

                        <div class="mt-auto">
                            <div class="interaction-bar">
                                <div class="inter-item" id="likeBtn" onclick="toggleLike()">
                                    <i class="bi bi-heart" id="likeIcon"></i> <span id="likeCount">0</span>
                                </div>
                                <div class="inter-item" id="favBtn" onclick="toggleFavorite()">
                                    <i class="bi bi-star" id="favIcon"></i> <span class="small">收藏</span>
                                </div>
                                <div class="inter-item">
                                    <i class="bi bi-chat-dots"></i> <span class="small">评论</span>
                                </div>
                            </div>
                            <div class="comment-box">
                                <input type="text" id="commentInput" class="form-control comment-input" placeholder="与藏友交流心得...">
                                <button class="btn btn-sm text-warning fw-bold px-3" onclick="submitComment()">发送</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="modal fade" id="publishModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-body p-4">
                    <form id="postForm">
                        <div class="text-center mb-4">
                            <h3 class="brand-font text-warning">拾珍 · 记事</h3>
                            <div class="small text-secondary">记下此间瓷器的神韵</div>
                        </div>
                        <div class="mb-3">
                            <label class="small text-secondary mb-2">图片上传 (支持多选)</label>
                            <input type="file" name="images" multiple class="form-control publish-form-control">
                        </div>
                        <div class="mb-3">
                            <label class="small text-secondary mb-2">请输入标题</label>
                            <input type="text" name="title" class="form-control publish-form-control" required>
                        </div>
                        <div class="mb-3">
                            <label class="small text-secondary mb-2">描述瓷器的器型、釉色、开片或背后的故事...</label>
                            <textarea name="content" class="form-control publish-form-control" rows="4" required></textarea>
                        </div>
                        
                        <button type="submit" class="btn btn-warning w-100 rounded-pill py-2 fw-bold mt-2">发布您与瓷的故事</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.js"></script>
    <script src="/common.js"></script>
    <script>const currentUser = JSON.parse('<%- JSON.stringify(user || null) %>');</script>
    <script src="/js/community.js"></script>
</body>
</html>